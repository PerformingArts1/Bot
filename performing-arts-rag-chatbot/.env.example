# Backend Environment Variables
OLLAMA_HOST=http://localhost:11434
LLM_MODEL=llama2
EMBEDDING_MODEL=nomic-embed-text
LLM_TEMPERATURE=0.7
LLM_TOP_K=40
LLM_TOP_P=0.9

# Frontend Environment Variables (Vite uses VITE_ prefix)
# This should match the backend URL if running locally
VITE_REACT_APP_API_BASE_URL=http://localhost:5000
